# Interfacing with Memory
Memory in code looks like an array
Memory physically is inferred RAM - specialized RAM blocks are used which are not registers

**Memory Mapped** - specific devices that have a register map inside your memory
- Switches
- Pushbuttons
They have a dedicated location in memory where you can access those interfaces/base address
Need to use C Pointer to point to base address, and then offset to communicate to all other locations

**Full Address Decoding**
Using all the address bits to decode a memory device
uses more hardware to decode all address
no aliases of memory
- every memory location is accessible from only one address

take a 16 bit decoding,
top four bits are used for memory decoding - These four bits splits it into sixteen blocks
take hex 0x3A7B
0x3 is the decoding - the device we want to access
0xA7B is the memory
We break it up like this due to space constraints.

**Partial Address Decoding**
Not all address bits are used to decode memory device
- Unused bits are don't cares in address decoding
Typically simpler decoding logic
Unused bits causes memory fold back or aliases
- memory locations are accessible from multiple locations
- Sampling below required rate (typical aliasing) but in VHDL is basically focusing on the bits you care about
Waste memory space

Our example from earlier but with partial address decoding - split the last four bits in half
00, 01, 10, and 11 will all have the same information. (3 downto 2)

