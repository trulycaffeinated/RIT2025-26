#include "io.h"
#include <stdio.h>
#include "system.h"
#include "alt_types.h"
#include "sys/alt_irq.h"

typedef unsigned char   uint8;  // 8-bit unsigned
typedef unsigned short  uint16; // 16-bit unsigned
typedef unsigned long   uint32; // 32-bit unsigned

// Pointers for direct mem access
volatile uint32 *leds = (uint32 *) LEDS_BASE;
volatile uint32 *buttons = (uint32 *) KEY_BASE;
volatile uint32 *ram = (uint32 *) INFERRED_RAM_BASE;

volatile uint8 stop_test = 0;  // Flag to stop test

int byte_accessible_test(uint32* start_addr, uint32 num_bytes, uint8* test_data){

	// Write Test Data to ram
	for(uint32 i = 0; i <= num_bytes && !stop_test; i++){
		*(start_addr + i) = test_data;
	}

	printf("Reading data");

	// Read test data
	for(uint32 i = 0; i <= num_bytes && !stop_test; i++){
		printf("Reading " + i);
		if(*(start_addr + i) != test_data){
			*leds = 0xFF;
			stop_test = 1;
			printf("Failed at memory location : %h", (start_addr + i));
		}
	}

	if(stop_test = 1) {
			return 1;
	}
	return 0;

}

int word_accessible_test(uint32* start_addr, uint32 num_bytes, uint16* test_data){

	// Write Test Data to ram
	for(uint32 i = 0; i <= num_bytes && !stop_test; i++){
		*(start_addr + i) = test_data;
	}

	printf("Reading data");

	// Read test data
	for(uint32 i = 0; i <= num_bytes && !stop_test; i++){
		printf("Reading " + i);
		if(*(start_addr + i) != test_data){
			*leds = 0xFF;
			stop_test = 1;
			printf("Failed at memory location : %h", (start_addr + i));
		}
	}

	if(stop_test = 1) {
		return 1;
	}
	return 0;

}

int dwrd_accessible_test(uint32* start_addr, uint32 num_bytes, uint32* test_data){

	// Write Test Data to ram
	for(uint32 i = 0; i <= num_bytes && !stop_test; i++){
		*(start_addr + i) = test_data;
	}

	printf("Reading data");

	// Read test data
	for(uint32 i = 0; i <= num_bytes && !stop_test; i++){
		printf("Reading " + i);
		if(*(start_addr + i) != test_data){
			*leds = 0xFF;
			stop_test = 1;
			printf("Failed at memory location : %h", (start_addr + i));
		}
	}

	return 0;

}

int main() {

	// LED Hello world
	*leds = 0xAA;

	uint32 bytes = 4096;
	uint32 words = 8192;
	uint32 dwords = 16384;

	uint32 test_data = 0x12345678;

	int res = 0;

	res = byte_accessible_test(ram, bytes, test_data);


    return 0;
}
